<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	<h4 class="modal-title" id="myModalLabel">New Customer</h4>
</div>

<div class="modal-body" id="modal-body">
	
	<% if @customer.errors.any? %>
		<div id="flash_messages" class="alert alert-danger" role="alert">
			<strong><%= @customer.errors.full_messages.join(', ') %></strong>
		</div>
	<% end %>
	
	
	<%= form_for @customer, :remote => true, :html => {:class => "form-horizontal"} do |f| %>
		<%= render :partial => "customers/form", :locals => {:f => f}%>
	<% end %>
	
</div>

<div class="modal-footer">
	<%= button_tag(:name => :commit, :type => :submit, :class => "btn btn-primary", :form => "new_customer") do %>
		Create Customer
	<% end %>
</div>
