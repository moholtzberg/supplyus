<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	<h4 class="modal-title" id="myModalLabel">Edit Account Payment Service</h4>
</div>

<div class="modal-body" id="modal-body">
	
	<% if @account_payment_service.errors.any? %>
		<div id="flash_messages" class="alert alert-danger" role="alert">
			<strong><%= @account_payment_service.errors.full_messages.join(', ') %></strong>
		</div>
	<% end %>
	
	<%= form_for @account_payment_service, as: :account_payment_service, url: account_payment_service_path(@account_payment_service, search_params), :remote => true, :html => {:class => "form-horizontal", id: "edit_account_payment_service_#{@account_payment_service.id}"} do |f| %>
		<%= render :partial => "account_payment_services/form", :locals => {:f => f}%>
	<% end %>
	
</div>

<div class="modal-footer">
	<%= button_tag(:name => :commit, :type => :submit, :class => "btn btn-primary", :form => "edit_account_payment_service_#{@account_payment_service.id}") do %>
		Update Account Payment Service 
	<% end %>
</div>
