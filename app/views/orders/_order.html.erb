
<td><a href="#" data-toggle="tooltip" title="order_id_<%= order.id %>"><%= order.number %></a><div class="tooltip top" role="tooltip">
  <div class="tooltip-arrow"></div>
  <div class="tooltip-inner">
    <%= order.id %>
  </div>
</div></td>
<td><%= order.account.name unless order.account.blank? %></td>
<td><%= number_to_currency(order.total) %><%# number_to_currency(order.sum) unless order.sum.nil? %></td>
<td><%= number_to_currency(order.amount_shipped) %></td>
<td><%= number_to_currency(order.amount_fulfilled) %></td>
<td class="text-center"><%= order.completed_at.strftime("%m/%d/%y %I:%M %p") unless order.completed_at.nil? %></td>
<td class="locked"><%= order.locked ? "Yes" : "No" %></td>
<td>

	<div class="dropdown">
		<button class="btn btn-default btn-xs dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
			<span class="caret"></span>
		</button>
		<ul class="dropdown-menu pull-right" aria-labelledby="dropdownMenu1">
			<li><%= link_to("View", order_path(order.id))  %></li>
			<% unless order.locked %>
			<li><%= link_to("Edit", edit_order_path(order.id)) %></li>
			<li><%= link_to("Lock", lock_order_path(order.id), :remote => true, :method => "put") %></li>
			<% else %>
				<% unless order.shipped %><li><%= link_to("Ship", new_order_shipment_path(order.id))  %></li><% end %>
				<% unless order.fulfilled %>
				<li><%= link_to("Fulfill", new_order_invoice_path(order.id))  %></li>
				<% else %>
				<li><%= link_to("View Invoices", invoices_path(:order_id => order.id))  %></li>
				<% end %>
			<% end %>
		</ul>
	</div>

</td>