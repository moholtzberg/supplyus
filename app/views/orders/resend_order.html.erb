<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	<h4 class="modal-title" id="myModalLabel">Resend Order</h4>
</div>

<div class="modal-body" id="modal-body">
	
	<% if flash[:error] %>
	<div id="flash_messages" class="aelrt alert-danger" role="alert">
		<strong><%= flash[:error] %></strong>
	</div>
	<% end %>
	
	<%= form_for @order, url: resend_order_confirmation_order_path(@order.id), method: "post", :remote => true, :class => "form-horizontal" do %>
	
		<div class="input-group">
			<%= label_tag :to, "TO:", :class => "input-group-addon" %>
			<%= email_field_tag :to, @order.email, :class => "form-control" %>
		</div>
	
	<% end %>
	
</div>

<div class="modal-footer">
	<%= button_tag(:name => :commit, :type => :submit, :class => "btn btn-primary", :form => "edit_order_#{@order.id}") do %>
	Send Order
	<% end %>
</div>
