<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	<h4 class="modal-title" id="myModalLabel">Edit Discount Code</h4>
</div>

<div class="modal-body" id="modal-body">
	
	<% if @discount_code.errors.any? %>
		<div id="flash_messages" class="alert alert-danger" role="alert">
			<strong><%= @discount_code.errors.full_messages.join(', ') %></strong>
		</div>
	<% end %>
	
	
	<%= form_for @discount_code, url: discount_code_path(@discount_code, search_params), :remote => true, :html => {:class => "form-horizontal"} do |f| %>
		<%= render :partial => "discount_codes/form", :locals => {:f => f}%>
	<% end %>
	
</div>

<div class="modal-footer">
	<%= button_tag(:name => :commit, :type => :submit, :class => "btn btn-primary", :form => "edit_discount_code_#{@discount_code.id}") do %>
		Update Discount Code
	<% end %>
</div>
