<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	<h4 class="modal-title" id="myModalLabel">New Address</h4>
</div>

<div class="modal-body" id="modal-body">
	
	<% if @address.errors.any? %>
		<div id="flash_messages" class="aelrt alert-danger" role="alert">
			<strong><%= @address.errors.full_messages.join(', ') %></strong>
		</div>
	<% end %>
	
	<%= form_for @address, as: :address, url: addresses_path(term: params[:term], account_id: params[:account_id]), method: :post, :remote => true, :html => {:class => "form-horizontal"} do |f| %>
		<%= render :partial => "addresses/form", :locals => {:f => f}%>
	<% end %>

</div>

<div class="modal-footer">
	<%= button_tag(:name => :commit, :type => :submit, :class => "btn btn-primary", :form => "new_address") do %>
		Create Address
	<% end %>
</div>
